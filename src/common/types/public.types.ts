export enum Gender {
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    NONE = 'NONE'
}
export enum statusUser {
    CONFIRMED = 0,
    CANCELED = 1,
}
export enum statusAdmin {
    CONFIRMED = 0,
    CANCELED = 1,
}